'use client'

import { ComingSoon } from '@/components/coming-soon'

/**
 * 4.1.5.2 Crew Pairing — Gantt Chart
 *
 * Aircraft-row timeline visualization for building pairings by dragging and
 * selecting flights across a period. Reuses the shared Canvas Gantt primitives
 * at `apps/web/src/lib/gantt/`. Ported from v1 `pairing-gantt-v2/`.
 *
 * Full implementation pending — see PAIRING_MASTER_PLAN.MD Phase 3.
 */
export function PairingGanttShell() {
  return <ComingSoon />
}
